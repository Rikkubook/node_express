<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% for(let i=0; i < data.length; i++) { %>
        <li>
          <%= data[i].name %>
          <%= data[i].photo %>
        </li>
      <% } %>
    </ul>
  </body>
</html>
